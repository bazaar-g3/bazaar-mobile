/**
 * Constantes compartidas para el filtro de rango de precios.
 *
 * PRICE_MAX_LIMIT debe ser mayor que cualquier precio existente en el catálogo.
 * Si se agregan productos con precios superiores a este valor, aumentarlo aquí.
 */
export const PRICE_MIN_LIMIT = 0;
export const PRICE_MAX_LIMIT = 500_000;
export const PRICE_STEP = 500;
